import { baseOneIndex } from '../../../helpers/base-one-index';
import { module, test } from 'qunit';

module('Unit | Helper | base one index');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = baseOneIndex(42);
  assert.ok(result);
});
